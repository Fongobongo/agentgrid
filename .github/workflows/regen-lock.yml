name: Regen lockfile (otel branch)
on: [push]
jobs:
  regen:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262
        with:
          ref: ${{ github.head_ref || github.ref_name }}
      - uses: dtolnay/rust-toolchain@4cda84d5c5c54efe2404f9d843567869ab1699d4
      - name: update lockfile entries for the otel stack
        run: cargo update -p opentelemetry -p opentelemetry_sdk -p opentelemetry-prometheus -p prometheus 2>&1 | tail -20
      - name: commit regenerated lock
        run: |
          git config user.name "agentgrid-ci"
          git config user.email "ci@agentgrid.local"
          git add Cargo.lock
          if git diff --cached --quiet; then
            echo "lock already up to date"
          else
            git commit -m "chore: regenerate Cargo.lock for the otel 0.32 stack"
            git push
          fi
